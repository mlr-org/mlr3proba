#' @section Parameter details:
#' - `integrated` (`logical(1)`)\cr
#' If `TRUE` (default), returns the integrated score (eg across
#' time points); otherwise, not integrated (eg at a single time point).
