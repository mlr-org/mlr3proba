# context("density.hist")
#
# test_that("autotest", {
#   learner = lrn("density.hist")
#   expect_learner(learner)
#   result = run_autotest(learner)
#   expect_true(result, info = result$error)
# })
